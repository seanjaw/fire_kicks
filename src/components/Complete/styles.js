import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({

    empty: {
        textAlign: 'center',
        padding: '60px',
        fontSize: '30px'
    },
    checkmark: {
        width: '100px',
        height: '100px'

    }
  }));
